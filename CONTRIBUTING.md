# 기여 지침

이 저장소는 공개 사양과 재배포 조건을 확인할 수 있는 자료를 바탕으로 독립 구현한다.
기여자는 코드·문서·데이터·이미지·테스트 fixture를 제출할 권한이 있어야 한다. 이 절차는
법률 판단을 대신하지 않으며, 권리 관계가 불명확하면 제출하지 않고 먼저 검토한다.

## 허용되는 입력

- 정부·표준화 기관·서비스 제공자가 공개한 API와 데이터 사양
- 공식 프레임워크·브라우저·라이브러리 문서
- 호환 가능한 라이선스와 원문 위치를 확인한 오픈소스
- 저장소를 위해 새로 작성한 요구사항, 합성 fixture, 코드와 자산
- 재배포 조건·버전·원본 해시를 기록한 공개 데이터

## 제출하지 않는 내용

- 이전 또는 현재 고용주·고객의 비공개 코드, 설계, 문서, 자산과 데이터
- 출처를 설명할 수 없는 코드·이미지·지도·아이콘·fixture
- 기존 구현을 옆에 두고 파일·함수·변수 이름만 바꾼 변환물
- 라이선스 또는 재배포 조건을 확인하지 않은 의존성·데이터
- API 키, Secret, 실제 사용자·사업자 데이터와 운영 응답 snapshot

## 독립 구현 절차

1. 공개 요구사항과 허용되는 입력 사양을 먼저 적는다.
2. 상태·오류·시간·좌표·크기 제한과 완료 조건을 테스트 가능한 계약으로 정의한다.
3. 기존 내부 구현을 변환하지 않고 새 모듈 경계와 데이터 모델로 작성한다.
4. 실제 운영 응답 대신 최소 합성 fixture로 정상·경계·실패 조건을 검증한다.
5. 외부 코드·자산·데이터를 추가하면 버전, 라이선스, 원문 URL과 고정 해시를 함께 기록한다.
6. `docs/reimplementation-log.md`에 입력 사양, 새 설계와 검증 명령을 남긴다.

파일명이나 함수·변수명의 차이는 독립 구현의 근거가 아니다. 공개 입력 사양, 설계 기록,
합성 테스트와 출처 증빙을 함께 검토한다.

## 변경 전 확인

```bash
node --test scripts/publication-rules.test.mjs
node scripts/audit-publication.mjs
./gradlew test
npm test --prefix cloudflare
npm run check --prefix tools/geodata
npm test --prefix e2e
git diff --check
```

추적 파일에는 로컬 Secret을 넣지 않는다. 개발용 값은 ignore된
`src/main/resources/application-local.yml` 또는 `cloudflare/.dev.vars`에만 두고,
노출이 의심되면 값을 출력하거나 다시 커밋하지 말고 즉시 폐기·재발급한다.
이전 회사·고객·내부 프로젝트 식별자는 `docs/publication-denylist.md` 절차에 따라
Git에 포함되지 않는 로컬 목록으로 추가 검사한다.

## Pull request 기록

PR에는 다음 내용을 남긴다.

- 공개 요구사항과 사용한 문서·데이터·라이브러리 원문 URL
- 새로 설계한 경계와 기존 공개 동작에서 의도적으로 달라진 부분
- 추가·변경한 합성 테스트와 실행 결과
- 제3자 라이선스·고지·자산 manifest 변경 여부
- 알려진 기능 공백 또는 운영 검증 조건의 변경 여부

프로젝트 자체 코드의 루트 라이선스와 권리 검토가 끝나기 전에는 외부 기여를 받지 않는다.
공개 전환 뒤에도 출처와 제출 권한을 확인할 수 없는 변경은 병합하지 않는다.
